# Smart Task Manager

Local repository for the Smart Task Manager project.

Contains the existing `task_manager.cpp` source file.

## Quick start

- Build with your preferred C++ toolchain (e.g., `g++ task_manager.cpp -o task_manager`).
- Run `./task_manager` after building.

## Files

- `task_manager.cpp` — main source file already in the project root.
